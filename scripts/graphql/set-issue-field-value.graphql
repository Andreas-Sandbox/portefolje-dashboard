mutation($issueId: ID!, $fieldId: ID!, $optionId: ID!) {
  createIssueFieldValue(input: {
    issueId: $issueId
    issueField: {
      fieldId: $fieldId
      singleSelectOptionId: $optionId
    }
  }) {
    clientMutationId
  }
}
